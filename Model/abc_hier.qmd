


```{r}



group_prior=abc_2M_p001 = readRDS(here::here("data/abc_2M_rmse_p001.rds"))
names(group_prior)
map(group_prior, class)

str(group_prior$abc_ev[[1]])


tetr<- group_prior$abc_ev$teter_results

ggplot(tetr, aes(x = c, y = lr, color = distance)) +
  geom_point()



```

